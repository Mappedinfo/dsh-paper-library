# Changelog

本项目采用语义化版本。每个发布版本在 GitHub Releases 与 Git 标签中同时提供。

## 未发布

### 研究难点挖掘（P1–P3）

- **P1 候选扫描（无模型）**：显式选择 1–50 篇，按小节边界做触发词/句式扫描，输出带真实页码与命中规则（`en-limitation`/`zh-open` 等 10 条）的候选段落；全程不发网络、不调用模型。
- **P2 单篇受限抽取（模型）**：把候选冻结为不可变来源，经隔离 DSH 子代理产出难点记录（`gap`/`question` + `source_status` + 逐字证据），保存为待核对草稿；引用校验失败不落盘，重复请求不重放。
- **P3 跨篇主题聚合（确定性优先）**：按归一化标签聚合成语料级主题草稿，给出覆盖篇数、年份分布、来源状态计数与代表引用；相似标签只产出待审合并建议，主题接受/否决/合并均需用户显式复核。可选模型合并建议有界且同样待审。
- **「研究难点」面板与导出**：库级面板显示 P1 回执、P2 状态与 P3 主题；导出 `exports/challenges.csv`、`challenges.md`、`challenges.bib`（默认只含已接受主题，缺失元数据字段省略）。

## v0.1.0 — 2026-09-16

首个公开发布：独立的 DeepSeek Harness 资料库插件（不依赖 Zotero 运行时）。

### 文献库与阅读

- 拖入 PDF、粘贴 DOI/arXiv/公开链接自动下载、解析、命名并保存管理副本；支持 CSL JSON、Zotero/Better BibTeX JSON、RIS、BibTeX 导入与去重。
- SQLite + FTS5 磁盘目录：按标题、作者、年份、DOI、引用键、标签、摘要检索；表格排序、分页、回收站与恢复。
- 连续 PDF 阅读：最多常驻三页图像与文字层，页码跳转、全屏阅读；**阅读区与批注栏之间的分隔条可无级调整宽度（160–720 px，方向键可微调）**，**PDF 支持全宽自适应与 25–400% 任意缩放**（放大后横向滚动，渲染密度随缩放提高）；宽度与缩放保存在主机并在刷新后恢复。
- 批注写成标准 PDF 对象（`/Highlight`、`/Underline`、`/StrikeOut`、`/Text`），PDF 是权威数据源；写入前备份、校验后原子替换。
- **文中批注与批注栏双向联动**：点击标记定位并高亮条目，点击条目滚动到文中并闪示；拖选文字优先，不干扰批注流程。

### 对话、知识与自动化

- 每篇论文复用一个 DSH 原生会话；批注以不可变快照作为引用材料（默认 24,000 字符预算），AI 回复可自动写回 PDF 源批注下并保留会话/消息溯源。
- 后台自动整理：有界并行（`analysisConcurrency`，默认 2），每批最多 8 页 / 24,000 字符，逐批落盘；完成后生成结构化**精读笔记草稿**（待核对）。
- 实时伴学（可选，默认关闭）：保存带评论的批注后按连续阅读方式回应，回复写回 PDF；单任务串行、1.8 秒合并、不自动重放不确定结果。
- 数据集与知识工作流：独立数据集/版本/关联、受界文件预览（100 行 × 50 列 × 512 KiB）、可审阅的知识图谱草稿与只读结构检查（`knowledge_draft_lint`）。
- 「构建引用库」：在文献库 `exports/` 原子写入 `references.bib` 与身份审计；缺 DOI 条目区分"有链接可补全/只能手工"；可选逐字段 DOI 核验（题名/年份/期刊/第一作者，只比较不回写）。
- 可选回环 `translation-server` 供应层：主路径取不到元数据时经 Zotero translators 重试，仍须通过精确 DOI/题名门禁；该服务不打包、不启动，只接收标识符与公开链接。

### 边界与不宣称

- 无全库扫描、无后台 OCR、无向量检索；扫描件与图像不计入结论。
- AI 生成内容始终标记待审阅；不编造作者、日期或 DOI。
- 网页抓取、翻译与整理使用模型额度；隐私文档默认不外发。
- 容量按约 2,000 条记录 / 1,000 个 PDF 设计验证；未验证 WPS 往返、真实大库容量与提供商输出质量。

### 许可

- 本项目原创代码为 [MIT](LICENSE)。默认 PDF 引擎 PyMuPDF 为 AGPL/商业双许可，CSL 处理使用 citeproc-js 的 AGPL 选项，内置 CSL 资产为 CC BY-SA 3.0，可选的 Zotero translation-server 为 AGPL-3.0（由部署方单独运行）。完整安装并非仅受 MIT 约束，详见 [THIRD_PARTY.md](THIRD_PARTY.md)。

### 验证

- 405 项 JavaScript 与 193 项 Python 测试，加上真实 Chromium 流程与原生 DSH 集成回执；全部使用合成文档，无真实提供商请求。范围与复现方式见 [docs/validation.md](docs/validation.md)。
