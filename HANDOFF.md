**Current empty-shape fix (2026-09-20):** the owner hit 「第 4 个节点既没有文本也没有文献。」 in the sidebar with several `（空）` shapes on the canvas: a shape tool click creates a node and opens the inline editor *before* anything is typed, and leaving that editor empty kept a node with neither text nor paper — which the host refuses — so every later save of the **whole board** was rejected while the reader's other work stayed unsaved. The canvas now honours the rule the host enforces: leaving the editor empty discards the shape (with its edges, undoable with Ctrl/⌘+Z); saves are held while an edit is open and resume when it commits; closing the board, switching boards or unloading the page commits the open edit first (text used to be lost when the board closed mid-edit); any stray empty node is pruned before a write and reported as 「忽略了 N 个没有内容的节点」 rather than failing the board; and a node-level rejection selects the node the host names with an actionable hint. Reader-side recovery for a board already stuck in a page running the old code: name or delete the `（空）` shapes and let the next autosave run, then reload. Validation: **497 JavaScript / 220 Python** tests (panel cases for the discard, the held write, the close-time commit, the prune and the actionable rejection) and the [31-check browser receipt](validation/board-browser.json), which draws a shape, leaves it unnamed and asserts the board still saves.

**Current Mermaid import and export (2026-09-20):** asked whether Mermaid could be parsed into our internal structure — it can, and without the `mermaid` package: that dependency is a renderer plus dagre, while what is needed is the text understood, and this plugin adds no runtime dependency. `web/board-mermaid.js` parses a bounded subset into the whiteboard's **own source document** plus a layout direction, so a pasted diagram travels the existing path (`fromSource` → host validation → 「校验并应用」) and the reader can inspect and edit the parse before it reaches the canvas; 「画板导为 Mermaid」 writes the canvas back as `flowchart` text for READMEs. Understood: shapes (`[]`, `()`, `(())`, `{}`, `>…]`, `[[]]`, `[()]`, `[[//]]`), link styles (`-->`, `---`, `-.->`, `-.-`, `==>`, `===`, `<-->`, `o--o`, `x--x`), both label forms (`-- text -->` and `-->|text|`), `&` groups, chained statements, `<br/>`, entities, quoted ids, `%%` comments and a `direction` override. Not guessed: `classDef`/`style`/`linkStyle`/`click` are ignored, `subgraph` is flattened, `~~~` is dropped, thick and circle endpoints import as documented approximations, identical links merge, and every note names its line. The new module is a genuinely new static asset, which the plugin's HTTP layer serves from an **explicit allowlist** — forgetting that entry 404s the module silently (the browser receipt caught it: 「这个页面没有加载 Mermaid 模块」). Validation: **495 JavaScript / 220 Python** tests, 7 new unit tests (subset, approximations, errors with line numbers, host-validator parity, round trip) plus a panel case, and the [30-check browser receipt](validation/board-browser.json) which pastes a diagram, applies it, asserts the record and the left-to-right placement, then exports it back.

**Current edge incidence angle (2026-09-20):** a line was clipped to its target's border along the centre-to-centre segment, so a nearly level pair (a wide, short paper node with the source just below its far corner) met the top edge at ~10° and the arrowhead read as lying along the node. Each end is now placed so the **drawn** segment meets the side it lands on at the edge's angle: perpendicular (90°) by default, adjustable in the inspector's 「夹角」 to 75/60/45/30, with the **30° floor** clamping anything shallower — the owner's "default perpendicular, adjustable, never below 30". The lean follows the centre line and the end clamps to the side (stopping at a corner instead of leaving the shape); because sliding one end moves the line the other measures against, the pair settles over a few passes (no-change pass ends it, four-pass bound). `angle` is an edge field in the board record and the readable source file, omitted at the perpendicular default, and the store validates integers 30–90. Verified by measurement rather than assumption: a deterministic sweep of 17,448 non-overlapping arrangements × five settings holds a worst incidence of 29.99° (coordinates round to 0.01 px; two shapes in the same place have no side to attach to, which is stated), and the 29-check browser receipt builds the grazing arrangement itself, measures the **drawn path**, and reports 90° now against **10.4°** with the old geometry. Validation: **487 JavaScript / 220 Python** tests, the [29-check browser receipt](validation/board-browser.json), the [14-check standalone receipt](validation/board-standalone.json) and the project receipt.

**Current node-drag label fix (2026-09-19):** dragging a node moved its shape and left its text behind — the labels (and the resize/connect handles) were drawn in absolute coordinates while the drag's incremental repaint, `redrawGeometry`, only rewrote the shape's geometry, so the text caught up on the next click (which triggers a full render). Each node now draws its children around its own origin inside a `board-node-content` group that carries the position as one `transform`; a drag rewrites that single attribute, a resize rewrites only the local size and the handle offsets, and the shape, labels and handles move as one. The browser receipt's drag check now asserts the label travels with the drag (±2 px of the 90×60 px move) and does not jump again on release; against the pre-fix file it fails with `moved by 0,0`. Validation: **485 JavaScript / 220 Python** tests, the [28-check browser receipt](validation/board-browser.json), the [14-check standalone receipt](validation/board-standalone.json) and the project receipt.

**Current whiteboard entry in DSH (2026-09-19):** the whiteboard is now an entry of its own in the DSH right sidebar rather than a surface hidden inside the library tab. One plugin registers two tab types with `ctx.sidebarRightTabs` — `paper-library` (文献库) and `paper-library-whiteboard` (画板) — each with a start-page capsule (`order` 15/16, with glyphs) and its own body in the `sidebar.right.pane.tab` seat, so both open as sibling tabs and can be docked next to each other. Registration is the same two-stage public path the shipped `files` type uses; a package may contribute several types, and the registry rejects only a duplicate `id` or a kind that cannot coexist. The whiteboard tab loads the very same page with `?view=board`, and the page then opens the board and enters focus mode — so that entry is a pure canvas, with no library, reader or annotation chrome, and Escape restores the rest of the page. Both panes keep the model/theme/settings/conversation bindings, so 「放入对话」 works from the board tab too. No second copy of the canvas exists. Two rounds of layout work followed the first look at it in the sidebar, both from the owner's report: the toolbar was two freely wrapping rows of ~35 controls, which became ~11 rows and 40% of the pane at sidebar width — it is now a tool row, a quick row and a 「更多」 panel that overlays the canvas, while `display:contents` flattens the same markup back into one inline toolbar on a wide screen (44 controls, 133 px against 180 px before; a 420 px pane keeps 121 px of toolbar and gives the rest to the canvas, which previously kept 280 px). And below 620 px the app is single-pane, so opening the board from the library stacked it *under* the shelf while `board-mode` hid the reader — half a canvas and no annotation rail; the shelf now steps aside whenever the board is open in a narrow container. The entry itself no longer flashes the library: it resolves at module scope (before the first paint) and opens the board right after `loadStatus()`, skipping the paper list and the reader restore. Validation: **485 JavaScript / 220 Python** tests, the extended client-bundle test (both types, their capsules, both iframe sources), the panel harness's 「更多」 toggle case, and the [28-check browser receipt](validation/board-browser.json), which drives `?view=board`, samples the boot to prove the library never paints, and measures both narrow-pane behaviours.

**Current reading projects (2026-09-19):** the reading list promised by the whiteboard phase is now real and lives where it belongs — in the managed catalog, not the private state store. Three tables (`projects`, `project_papers` with a composite primary key, `project_archive`) and SQLite `user_version` 3 back a genuinely many-to-many relation: a paper may belong to several projects and a project holds many papers, because a project is a queryable scope over the same papers rather than a container that owns them. That choice is what makes a project back up, search, export and reach the agent with the papers it describes. `project_create|update|get|list|archive|restore|link|unlink|for_paper` run through the normal dispatch path and are admitted by the bridge allowlist (adding them also fixed `未知文献操作。` rejections). Archive mirrors papers — an archived project disappears from `project_list` and stops claiming papers in `project_for_paper`, every mutation is refused with `已归档；恢复后才能修改或关联`, and its edges survive so a restore brings the members back. Linking an already-linked paper reports `linked:false,duplicate:true`; unlinking an unlinked one is a no-op; neither ever deletes or archives a paper. `resource_list` accepts an optional `project` filter that composes with query, sort, order, paging and `archived` (an unknown id is an error, never a silent unfiltered listing). The UI: a project bar (new/edit/archive plus a selector that scopes the library list), a 「项目 N」 button on the paper ribbon whose checkbox list ticks to join and unticks to leave with an inline "new project and join", and the board toolbar's project picker for `links.projects`. The agent uses the new `library_projects` tool (31 tools total). Two defects surfaced while verifying this in a browser: `openPaper` clears `state.active` while it awaits the catalog, so any control that read `state.active` at render time was a silent no-op if pressed in that window — the paper ribbon's project button, its board buttons and the board shelf's 「关联本篇／解除」 all now fall back to `state.openedId` and say so when nothing is open (the shelf case was what kept the P9 25-check board receipt red: the click produced no request at all); and the board's project controls were hidden during `bind()` — which runs before the first status reply — with nothing to reveal them, so that visibility now belongs to `setProjects`. Validation: **484 JavaScript / 220 Python** tests (including `tests-js/projects.test.mjs`), the [9-check project browser receipt](validation/project-ui.json) (`PLAYWRIGHT_MODULE=/path/to/playwright/index.mjs node scripts/project-ui-fixture.mjs`), and the whiteboard's own [25-check browser receipt](validation/board-browser.json) re-run after the board change.

**Current selection newline join (2026-09-19):** the reader used to insert a space between every captured word, so a selection spanning a line break gained a space at the wrap (very visible in Chinese PDFs, where one line is usually extracted as a single “word”). `joinSelection` in `web/pdf-reader.js` now joins by layout: Latin words on one visual line keep their space, a wrap contributes no separator (so `measure-` + `ment` stays `measure-ment`), and neighbours where either side is CJK never get a space. Rotation is handled by treating words that share left/right (vertical line on screen) as one line as well. Validation: **478 JavaScript / 220 Python** tests plus the [selection browser receipt](validation/selection-browser.json) (8 checks, now including a Chinese wrap that must not gain a space).

**Current PDF selection precision fix (2026-09-19):** the invisible text layer did not match the raster, so pointer hit-testing and the rendered highlight drifted along a line: words after the intended end were selected, and the old box-based clipping dropped words the browser had highlighted. `web/pdf-reader.js` now renders each word as an `inline-block` run fitted to its own word box (`scaleX(boxWidth / measuredTextWidth)`), adds explicit space runs for the inter-word gaps and a bounded trailing run at each line end, and captures text by **slice**: each overlapped word contributes the exact characters the range selects, with the horizontal extent from that slice's own client rectangles and the vertical extent from the raster word box. A word with selected text is never dropped. When a drag ends over a non-text area Chrome collapses the selection; the reader restores the last real range of that drag instead. The selection preview wraps and scrolls so a multi-line selection is shown in full. Validation: **477 JavaScript / 220 Python** tests plus the [selection browser receipt](validation/selection-browser.json) (7 real-Chromium checks: layer-to-raster alignment ≤1.5 px over 60 words, a 30-word two-line drag captured in full, the saved quote and one rectangle per crossed line, a gap release that stops at the previous word, a line-end release that keeps the line, and a rotated page).

**Current AI feedback write-back fix (2026-09-18):** asking for feedback on several annotations used to store one combined answer as a single PDF note whose `annotation_ids` listed every source annotation, and the rail therefore rendered the same answer under **all** of them. `feedback_context` now returns `mode:'per-annotation'` and requires one JSON entry per annotation id, `save_feedback` accepts `replies` and writes every reply in one atomic PDF replacement with `annotation_ids:[own id]` plus native `/IRT`, reporting unanswered ids in `missing` rather than filling them with another annotation's text. A model that answers with one combined text is stored once (`combined:true` + warning) instead of being copied, the strict parser refuses unknown/duplicate ids, empty or oversized comments and over-coverage before any write, and the reader rail builds threads in `web/annotation-threads.js` (per-annotation replies nest only under their own note; combined legacy feedback and conversation replies keep their shared label). Validation: **465 JavaScript / 215 Python** tests plus the [feedback browser receipt](validation/feedback-browser.json) (4 real-Chromium checks: per-annotation nesting, exactly-once combined answer, stored `/IRT` linking, no external requests).

**Current precise PDF selection fix (2026-09-17):** the continuous reader snapped a drag to whole word boxes, so a selection that started inside a word kept that entire word (reading as “from the start of the line” in tight layouts). `web/pdf-reader.js` now clips each overlapped word box to the browser's own selection rectangles (`Range.getClientRects()`) and takes the selected text slice per word via Range boundaries, then merges adjacent slices on a line as before; a word with no measured overlap still falls back to its whole box so degenerate layout data cannot drop it. `clipWords` is exported for unit tests (11 `pdf-reader` cases) and the reader browser receipt now has 24 flows, including a mid-word drag whose captured text must equal the browser selection and whose saved annotation quote is that exact slice. The level-panel trigger for challenge mining also moved from `.shelf-actions` to the catalog controls row, because the extra button squeezed the library search input to zero width at 741 px (the reader fixture's `#search` fill failed; the failure reproduced at HEAD before the move, and the panel-browser and catalog-layout fixtures now pass).

**Current whiteboard decoupling, links and focus (2026-09-18):** a board stays one independent file and is no longer conceptually tied to a paper: the record gained `links:{papers:[…],projects:[…]}` (many-to-many, associative only — linking copies nothing, unlinking leaves content untouched, bounded to 50 papers and 20 projects, and an empty list is dropped rather than stored). The readable source file carries the same `papers`/`projects` arrays, so one file expresses "what this board belongs to". The library pane gained a 「画板」 shelf that lists the host records (title, node/edge counts, linked counts), opens one, and links or unlinks it to the currently open paper through the host API — without switching the app into the board view. The paper toolbar gained 「＋ 画板」 (creates a board already linked to that paper) and 「这张论文的画板 N」. Those two controls are built in JS rather than living in the static markup, because the workbench module moves and then removes that block on every setup pass, and its destination is a collapsed citation menu. 「专注 ⤢」 is a pure-canvas mode: it hides the topbar, the library, the reader and the annotations (and the standalone page's own header/footer) without needing the Fullscreen API, so it works inside the DSH sidebar; Escape leaves focus before it clears a selection, and closing the board leaves focus too. `projects` was validated and round-tripped one phase before the projects themselves existed; that phase landed next (see the reading-projects entry above). Validation: **477 JavaScript / 215 Python** tests, the [25-check browser receipt](validation/board-browser.json) and the [14-check standalone receipt](validation/board-standalone.json).

**Current whiteboard editing, layout and source (2026-09-18):** the board gained real connection editing, automatic layout and a readable source pair. Edges now carry `kind` (`arrow|line|elbow`), `arrow` (`forward|none|both`), `dashed` and up to eight `waypoints`; a dedicated connect tool draws a line by clicking source then target (returning to selection afterwards), the inspector switches line style, and dragging a line inserts a bend point that Alt-click or double-click removes — a click without movement never re-renders, which is what keeps double-click on the handle working, and elbow paths hit-test against their drawn orthogonal corners rather than an invisible straight line. Automatic layout is deterministic in three modes — `tree` (respecting the reader's vertical order, four directions), `radial` and `layered` (longest-path layering with barycenter ordering) — with adjustable gaps; local coordinates are anchored to structure and normalised, so re-applying a layout is a no-op, and "钉住选中" nodes are excluded from every pass so a deliberate position survives. The board's source is now a readable pair: `board.json` (title, nodes, edges; short ids, stable key order, **no pixel coordinates**) and `board.style.json` (colours, sizes, fonts, per-relation edge styling, layout mode/gaps and pinned positions). The panel generates, edits, validates, applies, downloads and imports both; the standalone site renders a repository file directly with `?src=boards/example.json` and reuses the imported local copy on a second visit. Two contracts are now tested directly: `tests-js/board-source.test.mjs` asserts that everything the source module emits passes the host's own validator (which caught a style fill baked onto nodes), and the browser receipt drives the same gestures. Validation: **477 JavaScript / 215 Python** tests, the [25-check browser receipt](validation/board-browser.json), the [14-check standalone receipt](validation/board-standalone.json) and the [7-check native receipt](validation/board-harness.json). Chrome-side defects found and fixed during this round: elbow hit-testing, radial/layered idempotency, the connect tool swallowing the next gesture, a no-op drag that broke double-click, and glued error messages (`类型 notew 必须在…`).

**Current whiteboard (2026-09-18):** the plugin gained a literature whiteboard — a self-contained SVG canvas in its own `web/` app, with no new dependency, no model call while drawing or listing, and no background worker. A board is one private state record `board:<id>` (256 KiB cap, revision-checked writes, atomic replacement, deletion as a tombstone) with ≤400 nodes, ≤800 edges, 2,000-character node text and zoom 0.2–4; listing reads a window of at most 50 records and reports `{scanned,total,truncated}` rather than claiming completeness. The canvas supports pan/zoom, text/note/rect/ellipse/diamond nodes, drag and resize, connecting by dragging a handle, marquee multi-select, undo/redo, copy and delete, keyboard nudging and a real Fullscreen API. Papers drag from the visible library shelf onto the canvas as nodes bound to `paper.id` (title/year/citekey copied at drop time; a missing year is omitted, never guessed) and double-click back into the reader; a keyboard-reachable picker adds one or many, and "用勾选的文献新建画板" builds a new arranged mind map (theme root plus one node per paper). A selected knowledge-graph node joins the current board through the graph panel's own control (`onAddToBoard`), because the graph and the board occupy the same column and cannot both be visible; a paper-typed graph node keeps its paper binding and anything else becomes a concept node carrying only the label the graph showed. "整理成树" applies a deterministic tidy-tree layout (parent centred between first and last child, per-depth columns sized by the widest node, idempotent on a second arrange) and leaves unconnected nodes as further roots below.

The board is a reference source for the conversation: "放入对话" freezes a content-addressed snapshot (`board-ref:<sha256>`, immutable — editing or deleting the board never rewrites it), inserts a `[[paper-library-board:v1:<boardId>:<hash>]]` chip through the plugin's own `@` source (`paper-library-boards`, order 30, alongside annotations), and the host expands it at `agent/pre-step` into a `〔引用画板 …〕` marker plus a separate user message sourced `paperLibraryBoard` with a `body_hash`. At most four groups share the annotation character budget (24,000 default); a malformed, missing or identity-mismatched reference fails the turn instead of sending anything else. Board snapshots are deliberately not session-bound, because a board is cross-paper. The agent can work with boards through the 30th tool, `library_board` (`list|get|create|save|delete`); model additions and edits are marked `origin:'llm'` by diffing against the stored board, untouched content keeps the reader's authorship, a model-authored board stays `needs-review` until the reader saves it, and only the reader can call `board_accept`. Provenance is decided by the caller, never by the payload: a browser request is always a reader write, and a browser cannot request a model write or reach the `board:` namespace through the state API.

Four decisions were revised while building, each from an observed failure: the board occupies the workspace's second column (a fixed overlay hid the shelf and made drag-in impossible); accepting AI proposals is explicit (`board_accept`) because automatic saving would otherwise launder proposals into reader content; closing the board, switching boards or unloading the page settles the debounced write (a fixture reload inside the debounce window lost a node, and a pending save could also resurrect a deleted board); and a failed listing reports itself and creates nothing instead of fabricating an empty board. Validation: **475 JavaScript / 215 Python** tests, the [23-check browser receipt](validation/board-browser.json) (real Chromium, isolated standalone server, zero page errors and zero external requests) and the [7-check native receipt](validation/board-harness.json) (isolated DSH profile, deterministic keyless model: authenticated records, revision conflicts, a composer token expanding to frozen material, `library_board` offered to the model, an unresolvable reference failing the turn, and deletion preserving frozen material). Reproduce with `PLAYWRIGHT_MODULE=/path/to/playwright/index.mjs node scripts/board-browser-fixture.mjs` and `node scripts/board-harness-smoke.mjs`. The same `web/board.js` is also published as an independent static drawing service on GitHub Pages (`site/` plus `scripts/build-site.mjs`, deployed by `.github/workflows/pages.yml`): the board markup is extracted from the plugin page at build time so the two hosts cannot drift, `site/standalone.js` implements the same board actions over `localStorage` (40 boards / 4 MiB, reported in the footer) with JSON export/import and model-painted PNG export, and the library/composer controls are hidden rather than faked. Its [standalone receipt](validation/board-standalone.json) records 13 real-Chromium checks and the deployed site has a [live receipt](validation/board-pages-live.json) (6 checks over the public network, reproducible with `node scripts/verify-pages.mjs`); browser storage is convenience, not a durable store, and the exported JSON is the backup. Details in [docs/board-pages.md](docs/board-pages.md). Design and scope decisions are in [docs/whiteboard-design.md](docs/whiteboard-design.md); two whiteboard plugin names from an earlier assistant reply (`canvas-agent-dsh`, `dsh-ramify`) were checked against the public registry and do not exist, so nothing was installed.

The reused paper-chat harness profile could not be re-verified here: its first-turn generation count is 3 where the committed receipt records 2, and this reproduces on the pre-whiteboard baseline commit `8cbc82d`, with the new tool specification removed and with a fresh isolated home. That receipt is therefore left as previously verified and is not claimed as re-verified in this round; the whiteboard's own native receipt uses a fixture that does not depend on it.

**Current challenge mining P4 (2026-09-17):** `challenge_theme_check` is a read-only structural pass over stored themes with stable finding codes (`theme-without-evidence` error; `theme-single-paper`/`theme-inferred-only` warnings; `theme-unreviewed`/`theme-merged-member`/`theme-revised` info). `challenge_comparison` compares themes against a checklist the user owns — pasted text or a bounded local file (`.md/.markdown/.txt/.text/.json/.csv/.bib`, ≤1 MiB, source and UTC modification date recorded) — by deterministic token overlap (Latin words, CJK character bigrams) with `covered` ≥0.5, `partial` ≥0.2, `gap` below, and it stores the report per scope + checklist hash with a revision. It compares every stored status because it is a report, not an export, and it states that only wording was compared. `challenge_review_packet` writes `exports/challenges-review-packet.md` and `.json` atomically (findings, themes with coverage/years/counters/`[@citekey p.N]` quotes, the comparison table, and `provenance.review_actions` naming the exact review/merge/export payloads), refusing a comparison from another scope. The level panel gained 结构检查/对照清单/生成评审包 controls. Validation: **425 JavaScript / 213 Python** tests plus the [challenge panel browser receipt](validation/challenge-mining-browser.json) (10 real-Chromium checks) and the [native harness receipt](validation/challenge-mining-harness.json) (7 checks through an actual isolated DSH profile, model-free stages only). All four design phases are implemented.

**Current challenge mining P3 (2026-09-17):** `challenge_themes` aggregates saved `gap`/`question` records with `source_status` from accepted drafts into corpus themes (NFKC-normalised label keys, coverage/year/`source_status` counters, exact quotes with real pages, deterministic Jaccard≥0.6 merge suggestions that are never applied). Themes live in SQLite per corpus scope with a revision and `previous` summary; `challenge_theme_review` (user + expected revision) is the only status change and `challenge_theme_merge` folds reviewed themes into one new `needs-review` theme, marking members `merged`/`superseded_by`. `challenge_theme_suggest_start|get|cancel` are the optional model stage: bounded to 40 themes, they borrow the anchor paper's DSH route, carry only ids/labels/variants/counts (never PDF text), and their parse rejects unknown ids, duplicate members and malformed groups so nothing is auto-merged. `challenge_export` writes `exports/challenges.csv`, `challenges.md` and `challenges.bib` atomically with only `accepted` themes by default (8 MiB/file budget); BibTeX reuses the shared `bib_fields` mapping, so missing metadata is omitted. The corpus panel (`web/challenge-mining.js`, plus `web/challenge-mining.css` and the `src/http.mjs` asset allowlist) labels P1/P3 as model-free and gates P2/model suggestions on `status.challenge_mining`. Validation: **423 JavaScript / 208 Python** tests plus the [challenge panel browser receipt](validation/challenge-mining-browser.json) (7 real-Chromium checks). P4 (comparison report, review packet) remains.

# Project handoff

**Current reader markup modes and colour contract (2026-09-19):** the reading toolbar has a 选文后 switch: **勾选后提问** (default; a markup drag opens the annotation dialog) and **自动着色** (a drag writes the markup immediately with the current colour, no dialog — highlight while reading and ask later). Mode, colour and markup type persist in `reader:layout` through one serialized write queue, because overlapping read-modify-write patches used to race the revision and raise the save-retry banner. The selection wash follows the picker (`--pdr-selection` replacing the fixed blue/yellow rules in `pdf-reader.css`, `theme.css` and `style.css`), and a saved annotation flashes in its own PDF colour instead of teal. Validation: 484 JavaScript / 220 Python tests plus the [markup mode browser receipt](validation/markup-mode-browser.json) (7 real-Chromium checks).

**Current challenge mining P2 (2026-09-17):** `challenge_extract_start|get|cancel` (host module `src/harness/challenge-mining.mjs`) run the model stage over frozen candidate sources: one durable job per paper, one at a time, reusing the isolated spawn agent and the paper's authoritative model route. The draft JSON is validated before any write — `source_status` (`author-stated`/`inferred`) is required on gap/question nodes, evidence quotes must occur verbatim in the named frozen source, every gap needs an evidence assertion and every question a gap→question `limits`/`blocks` edge (the controlled vocabulary allows only claim/gap as assertion objects). Failed validation writes no draft; duplicate starts share one flight; a new request id is required to regenerate; cancellation aborts the owned call. `library_knowledge` now validates `source_status` and lints `challenge-without-evidence`, and the `library_challenges` native tool exposes `challenge_scan` (optionally persisting sources). Validation: **414 JavaScript / 200 Python** tests. P3 themes and P4 review/export remain.

**Current challenge mining P1 (2026-09-17):** `challenge_scan` implements the model-free stage of `docs/research-challenge-mining-design.md`: whole-line section recognition (EN/ZH, numbered or not, ending at back matter), abbreviation-safe sentence splitting, and a fixed 10-rule trigger lexicon producing page-anchored candidates with matched rule ids. Budgets: 50 papers, 200 pages and 24,000 characters per paper, 6 sections and 40 candidates per paper, 1,200 characters per candidate, 400 candidates / 200,000 characters per run; unreadable/archived/unknown ids are reported in `scope.skipped`. No model call and no network access is involved. Validation: 5 Python cases (`tests/test_challenges.py`) and 3 bridge integration cases (`tests-js/challenges.test.mjs`). P2 (bounded per-paper extraction), P3 (cross-paper themes) and P4 (review panel, lint, export) remain.

**Current research-challenge mining design draft (2026-09-17):** no corpus-level difficulty/limitation mining exists yet; the plugin only has per-paper graph/note drafts and a metadata-level bibliography build. `docs/research-challenge-mining-design.md` records the verified external basis (Oppenlaender & Hämäläinen, arXiv:2306.05036 / IJHCI — 879 CHI-2023 papers, 4,392 challenges, 113 topics, κ=0.97, ~US$50; BAGELS at Findings of EMNLP 2025; GAPMAP title-level only), the reuse map against existing code, a four-stage bounded pipeline (deterministic candidate scan → bounded per-paper extraction → cross-paper theme aggregation → review/export), and the invariants (`source_status` separation, exact quotes with real pages, review-only themes, no whole-library scan, no new service). A P1 spike on a synthetic paper located 8 sections and 5 trigger sentences with PyMuPDF only (zero model calls) and found that sentence segmentation must be section-aware because headings glue onto body text. Not implemented; P1/P2 await the owner's go-ahead.

**Current catalog-title jump (2026-09-16):** clicking a catalog table title now opens the reader directly (the workbench row keeps edit/archive actions for selection-only work; dataset titles still route to the dataset view). A metadata-only record shows its no-PDF surface without any page fetch; the earlier metadata-only title contract is superseded. Validation: **405 JavaScript / 193 Python** tests and reruns of the workbench (title click collapses the table and opens the no-PDF or reading surface) and reader fixtures (title-click identity across reader, fullscreen and library-collapse), with the reload-restore flush fixed in the reader fixture.

**Current v0.1.0 release (2026-09-16):** `CHANGELOG.md` records the first public release and `package.json` ships it in `files`; the annotated tag `v0.1.0` and the GitHub Release were created and read back (not draft, not prerelease). The upstream community showcase (Discussion #6623) was updated with one reviewed release comment: `docs/community/release-v0.1.0.md` plus the readback receipt `docs/community/discussion-comments.json`; `scripts/announce-release.mjs` posts only when no comment with the same body hash exists and verifies by re-reading, so repeated runs report `existing` instead of duplicating. npm publication remains intentionally disabled (`private: true`).

**Current annotation linking (2026-09-16):** saved markup in the raster and its rail entry are linked both ways. The page payload already carried annotation rectangles, so `pdf-reader.js` keeps them per rendered page and hit-tests clicks geometrically against the same display-space rectangles as the word layer — a text selection keeps priority, so drag-selection still opens the annotation flow. A click on markup flashes a transient, pointer-events:none ring and calls `onAnnotationActivate`; clicking a rail card calls `revealAnnotation`, which navigates to the page, centers the markup and flashes it. `linkAnnotationCard` scrolls and marks the matching card only when the annotation rail is visible; no list rewrite, no second read. Validation: **405 JavaScript / 193 Python** tests and the [23-flow reader browser receipt](docs/validation/reader-browser.json) with two new real-Chromium flows (card click reveals+flashes markup, markup click links its card); sidebar, workbench, resource, annotation-reply and companion fixtures rerun and pass.

**Current free reader layout (2026-09-16):** the reading pane and the shared library/annotation rail are now independently resizable. A draggable separator (`#reading-rail-resizer`, also arrow-key adjustable, hidden on ≤620 px and in catalog mode) sets `--rail-width` on the workspace between 160 and 720 px; every media-query grid rule consumes `var(--rail-width, var(--rail-default, …))` so the user's width wins while breakpoints keep their defaults. The PDF reader defaults to fit-width and accepts arbitrary zoom (25–400 %): steppers, an explicit percentage input (Enter/change commits) and a 全宽 reset, with real horizontal scrolling and a raster density cap that rises with zoom (up to 4×, zoom 1 keeps the previous 2× behaviour). Both the rail width and the zoom persist in the host `reader:layout` record and are restored on reload; the PDF re-fits automatically after a drag because the reader observes its own box. Validation: **405 JavaScript / 193 Python** tests, including two new panel cases for drag/clamp/persist/restore and the [21-flow reader browser receipt](docs/validation/reader-browser.json) with three new flows (drag + refit + persist, percentage zoom + horizontal scroll + reset, reload restore). Sidebar, workbench, theme, companion, analysis, resource, bibliography and annotation-reply fixtures were rerun and pass.

**Current xref robustness fix (2026-09-16):** importing a real Nature PDF produced by incremental updates failed with `code=7: cannot find object in xref` because the write-path signature probe (`_open_pdf(writing=True)`) raised on freed xref slots whose objects no longer exist. The probe now skips exactly that MuPDF condition (a missing object cannot carry `/ByteRange`) while still failing closed on any other probe error; signed/encrypted/repaired refusals are unchanged. Regression coverage is synthetic (monkeypatched probe plus negative control); the real file was verified imported locally without entering fixtures. Validation: **403 JavaScript / 193 Python** tests pass.

**Current optional translation-server fallback (2026-09-16):** deployments may set `translationServer` to a loopback HTTP address (`cordis.patch.yml` env `DSH_PAPER_LIBRARY_TRANSLATION_SERVER`, standalone `--translation-server`). When the primary metadata path (Crossref/landing/arXiv) returns nothing, `metadata_lookup` retries the same target through a self-hosted Zotero translation-server: identifiers via `/search`, public landing URLs via `/web`. `src/translation-server.mjs` enforces loopback-only plain HTTP, POST-only bounded requests (4 KiB payload, 1 MiB response, 15 s) and conservative Zotero-item mapping (unknown values stay absent; unparsable dates dropped). Candidates pass the unchanged exact-DOI/exact-title identity gates; a sidecar draft records `provenance.provider:'translation-server'`, and failed gates surface as warnings. The AGPL-3.0 service is never bundled, installed or started by this plugin (recorded in THIRD_PARTY.md); identifiers/URLs only — no PDF text, notes or drafts. Validation: six new focused JS tests (config validation, mapping, fallback with identity gates, foreign-identity discard, unconfigured primacy, payload rejection) plus the full suite at **403 JavaScript / 192 Python**; stub transports only, no real service or network. This implements the layered-recovery supply layer from the user's metadata-recovery note; AI extraction stays the last resort with exact-quote field evidence, and identifier verification stays mandatory.

**Current field-level bibliography verification (2026-09-16):** the `bibliography_build` audit now splits DOI-missing records into `actionable.lookup_by_url` (a landing URL remains for the existing metadata-lookup path) and `manual_only`, and each audit record carries `has_url`. Explicit `verify:true` now compares four fields per record — title, year, container-title, first-author — each with `validation_status` (`match`/`conflict`/`missing-catalog`/`missing-online`/`missing-both`) and both stored and registered values preserved; records report `provider-confirmed`/`conflict`/`unavailable` plus `checked_at` and the registry source host. Comparison is normalized, reporting is as-stored; nothing writes back. This follows the per-field `value/source/checked_at/validation_status` advice in the user's metadata-recovery note; translation-server/GROBID supply layers remain uninstalled. Validation: 397 JavaScript / 192 Python tests plus the bibliography browser flows; deterministic transport fixtures only, zero real network or provider calls.

**Current live companion (2026-09-16):** a successful human HTTP annotation create/update now admits a host-owned companion job when the shared `auto-paper-conversation` setting is enabled. Bare highlights, AI replies, unchanged versions and explicit save-to-composer drafts do not generate. The 1.8-second debounce coalesces unsent edits; one automatic question is admitted at a time so separate comments cannot be silently batched into one native turn. The paper's normal DSH conversation/model receives an immutable annotation reference plus one page of text (7,000 characters, truncation stated). The continuous-reading policy distinguishes source, reader and inference, avoids forced quizzes and never treats explanation as mastery.

`companion-queue.mjs` persists at most 200 pending records and 100 recent statuses per paper in the existing host state store. Native completion writes the response under its source PDF annotation and settles the queue without a browser. Startup reconciles only sessions named by accepted jobs; queued work resumes, uncertain/interrupted generations require explicit recovery, and PDF-only failure retries do not regenerate. The reading toolbar and source cards expose the shared switch and status. Imported native `/IRT` replies explicitly labelled `AI 伴学回复` are recognized as AI while remaining external annotations. No author-name heuristic or feedback loop is introduced.

Validation: **397 JavaScript / 192 Python tests**, [16 actual native DSH flows](docs/validation/paper-chat-harness.json) with four deterministic generations, and [four companion browser flows](docs/validation/companion-browser.json), including page closure and restoration at 511/681 px. Tests contain synthetic documents only. The requested private PDF was copied and inspected under ignored local output; neither its content nor identity enters public fixtures. No real-provider response quality, WPS file watcher, full skill ledger integration, OCR or new memory benchmark is claimed. New-install auto-reply remains opt-in; explicit local setup may enable it prospectively without replaying historical notes. See [API](docs/api.md#live-reading-companion).

The linked local DSH and standalone preview were restarted, preserving the user's browser page/drafts. [Installed-host verification](docs/validation/companion-install.json) confirms authenticated companion capability and exact served assets. The user-requested [native setting readback](docs/validation/companion-settings.json) confirms automatic replies enabled with unrelated preferences preserved. Setup made no model call and admitted no historical notes; standalone preview still cannot generate. Private inspection outputs remain outside the package.

**Current reading notes and draft lint (2026-09-16):** a completed analysis run now also saves one pending-review **reading-note draft** (`mode:note`): the same isolated spawn condenses the committed batch graph projections (≤24,000 bytes, in batch order) into a structured Chinese note with a 待核实与未覆盖 section; partial batch coverage is recorded as `note_coverage` and stated in the note. Note sources are the snapshots actually quoted by the included batches within the knowledge budget; a note failure only adds a job warning. New read-only `knowledge_draft_lint` reports unsupported claims, isolated nodes, unused evidence, duplicate relations, dangling endpoints and sourceless observations; the review view exposes it behind an explicit 结构检查 button with one 429 retry, and `library_knowledge` gains the `draft_lint` operation. This round also fixed a pre-existing race where the knowledge workflow's asynchronous `show()` tail could re-render the note editor over an in-progress edit; an edit-epoch guard now skips the stale restore. Validation: **387 JavaScript / 191 Python** tests, [14 resource browser flows](docs/validation/resource-browser.json) including lint and three consecutive stable runs, reruns of analysis browser/harness (the native receipt now includes 2 deterministic note generations), sidebar and workbench flows. No real provider, OCR or real-library capacity claim.

**Current bounded-parallel analysis and canonical bibliography (2026-09-15):** paper analysis now runs distinct papers in parallel up to the deployment `analysisConcurrency` (1–4, default 2); batches within one paper remain serial, admission beyond the bound fails before any catalog read, and the durable queue drains with the same parallelism without changing its persistence format. The new `bibliography_build` action (workbench shelf button, `library_bibliography` native tool, JSON CLI) writes `exports/references.bib` and `exports/bibliography.audit.json` atomically inside the managed library. The audit reports factual identity only — citekey conflicts, duplicate DOI groups, missing citekey/DOI/title/author/year, and managed-PDF file presence — without opening PDFs or writing catalog rows. Explicit `verify:true` compares up to 100 paper DOIs per run against public registration (match/title-mismatch/unavailable), never writes back, and reports any unverified remainder. Validation: **385 JavaScript / 188 Python tests**, the [three-flow bibliography browser receipt](docs/validation/bibliography-browser.json), and reruns of the 13 workbench / 18 reader / 13 sidebar flows; verification tests use a deterministic transport fixture with zero real network or provider calls. No real-library capacity, OCR, cross-batch graph synthesis or provider-quality claim is made.

The annotation-reply update is installed locally. DSH and the standalone preview were restarted without configuration changes. The [installed-host receipt](docs/validation/annotation-replies-install.json) checks authenticated capabilities and byte-for-byte reply/menu assets against source, with no model requests or private document reads. The existing review browser was left untouched to preserve its unresolved drafts.

**Current annotation replies and toolbar (2026-09-15):** completed native turns with explicit logged annotation references automatically save their assistant response under the referenced PDF notes. Associations are checked against immutable snapshots; unreferenced later turns cannot inherit earlier annotations. `annotation-replies.mjs` folds only current-turn provenance. Native `session/event` triggers saving without browser polling; bounded history reads reconcile at most two recent missed replies, newest first. Host `paper.reply:*` pending/saved/failed receipts prevent silent retries or recreation after deletion; the UI exposes explicit save recovery. Duplicate old unassociated PDF copies can gain verified links without rewriting their body. Native same-page `/IRT`, all source IDs, and source snapshot/session/message IDs survive PDF copy/reimport. A multi-note response keeps one body and displays beneath each associated source; unmatched/deleted sources stay in an unlinked group. Counts exclude AI responses.

The citation/export workspace tab is removed. Its existing actions now live in a library/reading toolbar menu, retaining the view/page and closing on outside click/Escape. Current validation: **379 JavaScript / 183 Python**, [five reply browser flows](docs/validation/annotation-replies-browser.json), [14 native chat flows](docs/validation/paper-chat-harness.json), and reruns of [18 reader](docs/validation/reader-browser.json) / [13 workbench](docs/validation/workbench-browser.json) flows. Native verification uses two deterministic generations, proves automatic PDF reply saving before any browser history poll, and makes no real provider request. No whole-library scan, OCR, new memory comparison or formal user validation is claimed.

**Current full-paper automation (2026-09-15):** PDF imports/attachments and selected papers without prior jobs enter a durable serial host queue. `auto_analysis` and `analysis_fill` now default true; explicit saved user choices survive upgrades. Scope defaults to all pages, with optional page lists/ranges in the UI. `paper_analysis_batch` advances an exact page/text cursor; each worker reads at most eight pages and returns at most eight chunks / 24,000 characters. Long-page text continues without truncation. File-version nanoseconds are strings to preserve identity across Python/JavaScript JSON. Changed PDFs stop later batches and preserve completed work.

`paper-analysis-queue.mjs` persists only explicit import/selection admissions (at most 2,000 queued papers / 200 KB), runs one paper at a time, and recovers unstarted entries without scanning the library. Interrupted/failed/cancelled jobs never replay automatically. `paper-analysis.mjs` saves independent batch records/drafts and cumulative coverage; only one selected batch is loaded into the UI. Metadata fills after all batches, keeps existing values and records source-backed pending provenance. Cross-batch graph deduplication and OCR are not implemented. Scope, cursor, empty/changed sources, queue cancellation/recovery, defaults and existing features have focused checks; [native](docs/validation/paper-analysis-harness.json) and [browser](docs/validation/paper-analysis-browser.json) receipts now include 13-page automatic processing in two batches. Standalone preview still cannot invoke DSH models.

**Current shared-settings implementation (2026-09-15):** the plugin's top-right Settings button and DSH Settings → Plugins → Plugin configuration → Paper Library edit the same native `paper-library` namespace. The installed package appears in DSH's official plugin inventory; this is local inventory, not marketplace approval. Native settings own selection automation, fill-only metadata automation, annotation auto-send and sidebar placement. Models, theme, paths and execution permissions retain their existing DSH owners. See the [API contract](docs/api.md#shared-settings).

`src/harness/settings.mjs` registers the optional native namespace, migrates only previously unspecified values, preserves legacy records/backups, and uses native revision checks. The old preferences API projects the shared fields from that namespace. The keyed client settings card uses official `settingsScope`; the reading bridge sends revision-only invalidations. The internal dialog refreshes while visible and generation-fences late reads so they cannot restore disabled automation. No browser storage, model calls or catalog scans are required. Standalone deployments retain disk settings; a preview sharing a migrated library is explicitly read-only for settings and does not create a second DSH settings-file owner.

The full-paper milestone passed **375 JavaScript / 181 Python tests**, [nine analysis browser flows](docs/validation/paper-analysis-browser.json), [nine actual native analysis flows](docs/validation/paper-analysis-harness.json), and a rerun of [seven native/browser settings flows](docs/validation/settings-browser.json). Synthetic full-text runs cover 13 pages in two batches, completion after browser close, metadata fill, selected-batch restoration, restart and no main-conversation messages. No real provider quality or new memory measurement is claimed. The native settings card and authenticated plugin page are exercised; iframe invalidation has focused component coverage. Earlier feature receipts below retain their original scope.

The linked local DSH and preview services were restarted; [authenticated installed-host verification](docs/validation/full-analysis-install.json) confirms current capabilities. `scripts/enable-local-paper-analysis.mjs` uses the native revision-checked settings API to apply the user's two requested enabled choices; the [readback](docs/validation/full-analysis-settings.json) found both already true under the new defaults, so no override write was needed. Other preferences are preserved. Verification/setup scripts read no private documents and make no model requests. The user's existing browser page was not forcibly reloaded.

**Preserved selected-paper review implementation (2026-09-15):** catalog search now shares the 44 px heading, with a 28 px input. Fixed-width table cells clamp titles/authors to two lines and provide a complete-title hover/focus popover; narrow tables scroll internally. The [catalog receipt](docs/validation/catalog-layout-browser.json) covers 511/681/1400 px light/dark, bounded rows and keyboard search. The existing 13 shared-sidebar flows were rerun.

`paper-analysis-agent.mjs` uses actual native spawn with a private temporary parent, selected paper model, tool denial and owned cleanup. Sources remain bounded per batch; automatic full-paper processing supersedes the earlier three-page default described in Git history. All suggestions retain pending AI provenance. Selected result nodes can enter the correct paper's persistent conversation draft; generation itself never changes main history. [Native](docs/validation/paper-analysis-harness.json) and [browser](docs/validation/paper-analysis-browser.json) receipts use deterministic synthetic material, not private papers or real provider quotas. See the [API contract](docs/api.md#selected-paper-background-analysis).

Scope: PDF text from the requested scope, no OCR/network/full-library search; native background logs remain on the host. Closing a browser permits the owned job to finish, while uncertain model outcomes never auto-retry. Old knowledge drafts remain available when a newer job fails. Concurrency is bounded per host service; it is not a distributed scheduler. Standalone preview can read stored results but cannot independently proxy a DSH model.

The existing local DSH and review service were restarted after an authenticated idle check; configuration was preserved. [Installed-host verification](docs/validation/paper-analysis-install.json) confirms the selected-paper route, compact catalog assets and existing reader/reference/dataset/theme capabilities. The user's existing browser was not forcibly reloaded, and installation verification made no model calls or private-document reads.

**Accepted dataset and knowledge implementation complete (2026-09-15):** [Implementation contract](docs/library-datasets-knowledge-design.md). The unified catalog supports independent datasets/releases/assets, mixed citation import/export, bidirectional sourced paper relations, CAS and archive/restore. Explicit UTF-8 CSV/TSV/JSONL previews have 100-row/50-column/512-KiB output and separate input/line/field/time budgets. `paper-library-knowledge` and `paper-library-notes` use scoped immutable sources, native DSH model routing, typed drafts that require user review, and revisioned Markdown with crash recovery. No user library migration, background scan, real provider test or private skill registry import was performed.

The new [browser receipt](docs/validation/resource-browser.json) covers 13 real Chromium flows, including a second browser, saved-annotation change checks and explicit stale-record reconciliation. [Native Harness checks](docs/validation/resource-harness.json) cover seven authenticated flows with three deterministic local generations, dataset/release model inheritance and zero generation replay after restart. [RKOS subset validation](docs/validation/rkos-subset.json) invokes an explicitly selected upstream parser/build/lint on synthetic records; unsupported dataset source chains are retained in JSON and listed as losses. [Preview memory](docs/validation/dataset-preview-memory.json) measures only a short-lived worker reading a sparse synthetic file; it excludes the host/browser and is not a real-library capacity estimate.

The existing linked `web` profile and standalone review service were restarted after an authenticated idle check, preserving their configuration and ports. [Installed-host verification](docs/validation/resource-install.json) confirms new capabilities/assets and existing annotation/reference/reader/language/theme capabilities, without reading private documents or invoking a model. The original review page was not refreshed; a separate preview URL was queued for review.

Important scope: citation export is metadata, selected knowledge JSON is an exchange of accepted graphs/notes and referenced snapshots, and RKOS is a lossy compatibility subset. None is a complete files/sessions backup or whole-library restore. Data-file copy/upload, binary/GIS previews, whole-library extraction and a standalone-to-DSH proxy remain separate work. Legacy graph-node promotion is available via explicit API; the old reading graph UI remains separate.

**Current accepted work (2026-09-15, implemented):** library and annotations share one reading rail with two tabs and left/right placement. Switching tabs moves no reading content, preserves search/list scroll and keeps the PDF position stable. Cards use a two-line 12 px title, author/year and a compact journal/PDF/citekey row; complete information remains inspectable. Ctrl/Cmd + K reveals library search, and narrow/fullscreen close releases the rail. Metadata retains quick editing and conversation retains its floating panel. Existing host-owned content and side preference storage are unchanged. See the [design contract](docs/ui-review-design.md), [13 sidebar browser flows](docs/validation/sidebar-browser.json) and [live-asset check](docs/validation/sidebar-install.json).

The selected-paper milestone passed **333 JavaScript / 174 Python tests**; the [automated receipt](docs/validation/automated.json) now records the full-paper update above. The isolated Chromium sidebar fixture passes 13 flows in light/dark mode: synthetic cards remain within a 90 px limit, and 12 tab switches show zero PDF scroll offset. It records zero model calls, external requests, browser-storage writes or browser errors; synthetic originals are unchanged. The 12 theme and 18 reading browser flows retain their preceding shared-rail verification; specialized workbench/reference/language receipts retain their own scope. Details are in [validation](docs/validation.md).

**Preserved theme milestone (2026-09-15):** neutral DSH colors, system sans-serif typography and host-owned light/dark/font settings follow the official theme service and event; standalone follows the operating system. PDF pixels and annotation colors are unchanged. Its [12 browser checks](docs/validation/theme-browser.json) use actual DSH CSS and the real bridge with an isolated theme snapshot/event source, rather than the full native settings UI; its [installation receipt](docs/validation/theme-install.json) separately verifies authenticated assets.

**Preserved language milestone (2026-09-15):** selected PDF text can be translated or polished with the paper's authoritative DSH model. Source-grounded vocabulary retains paper/page/model provenance and editable meaning/mastery. Drafts, references, reading position, preferences, language results and vocabulary live in private DSH-home files shared by browsers. Verified legacy migration retains conflicting versions as recoverable backups. Evidence: [12 cross-browser flows](docs/validation/language-browser.json), [6 actual Harness checks](docs/validation/language-harness.json), and the then-rerun 17 reading, 13 workbench and 11 native-reference browser flows. Language fixtures each used two deterministic generations; reference used three. The reading fixture observed at most three resident images and one page request at a time over mixed-size/rotated synthetic PDFs. Previous 2,000-record/1,000-PDF memory receipts retain their original scope; none measures the theme update's total memory. Read README.md, the current workbench contract and [the reference contract](docs/annotation-reference-design.md) before continuing.

## Accepted goal and current result

Independent low-memory literature plugin in Mappedinfo, integrated with DeepSeek Harness. Provides quick import/search, APA 7 clipboard, individual/full-library BibLaTeX export, continuous PDF reading, native persistent annotations, a typed evidence graph and one native Harness conversation per paper. Zotero is optional input, not a runtime dependency or canonical store for this plugin.

The accepted reading workflow uses one reference collection per question in the same native conversation in the plugin and Harness main page. New/updated, manually selected and all-user-note shortcuts add to that collection; later saved notes prompt explicit addition. Search, page filters and pagination preserve selection. Temporary selected PDF text can coexist with saved notes. Opening a paper creates or reuses a durable cold Session without starting an Agent or model call. Initial model selection follows the source conversation's public projection or Harness default; later choices belong to the paper Session. Automatic sending remains opt-in and off by default. Complete AI replies can be saved individually to PDF with native Session/message provenance.

Current source checks exact selected PDF versions and full source text before writing a private immutable snapshot. Metadata selection holds at most 1,000 notes with 240-character text/comment previews; source preparation defaults to 24,000 Unicode characters, configurable from 1,000 to 96,000, and rejects overflow rather than truncating. A frozen canonical token works through either native input or the plugin. The public Host pre-step extension logs exact source content using a generic `Paper Library` plugin context, with identity/version/hash metadata outside model text. Only that verified, durably flushed log evidence updates the native usage projection; queue acceptance or a plain user token alone does not. The projection retains at most 5,000 last-sent annotation versions and reports eviction.

The added intake requirement is implemented: drop/select multiple PDFs or paste links/DOIs to fetch, parse, name and save managed copies; embed an independent DSH adaptation of `paper-fetch-skill` named `paper-library-fetch`. The skill uses the calling session and plugin tools. Exact parsed-title evidence gates online bibliography enrichment; metadata-only acquisition and incomplete parsing remain explicit. No external browser/MCP fetch runtime is installed.

User capacity target is approximately 2,000 records and 1,000 PDFs. Source Zotero data and existing PDFs were not migrated or modified during implementation.

## Ownership and architecture

- `datasets.py` owns additive dataset/release/asset/link schema, external IDs, cross-resource citation identity, bounded unified queries and consistent paginated citation exports. `dataset_preview.py` handles one explicit source preview; no data reader runs from catalog search.
- `library_knowledge.py` owns fixed source snapshots and reviewable graphs in SQLite, immutable Markdown revisions and write intents. `harness/library-knowledge.mjs` checkpoints generation requests in DSH-home state, replays durable output without duplicate AI calls and never auto-accepts output. `resource-tools.mjs` forbids model-side review/note overwrite and forces model links/drafts to pending origin/status.
- `web/resource-library.js` and `knowledge-workflow.js` route independent items and explicit-source knowledge UI; keep their drafts on the host. The three bundled project skills register lazily through the native registry. The public skill provenance manifest records the independently authored adaptations; do not add private upstream directories.

- `src/dsh_paper_library/`: disk SQLite/FTS5 catalog and short-lived PDF worker; annotations are read from managed PDFs, not database annotation rows. `bibliography.py` owns the factual identity audit and atomic `exports/` writes; it never opens PDFs or mutates catalog rows.
- `src/bridge.mjs`, `citation-worker.mjs`, `citations.mjs`: bounded job dispatch, independent CSL/BibLaTeX work. Citation.js must retain explicit source citekeys; do not restore autogenerated author/year labels.
- `src/paper-fetch.mjs`, `import-pdf.mjs`, `http.mjs`: public acquisition, title-checked metadata enrichment, source receipts and raw streaming upload. Limits cover redirects/DNS, bytes, deadlines, concurrency and aggregate queued payload; never reinstate whole-PDF Base64 in the browser. `skills/paper-library-fetch/` is the packaged adaptation; provenance is in `docs/upstream-manifest.md`. Preserve its distinct runtime name: the upstream name is shadowed by the user's existing global skill in actual Harness sessions.
- `src/harness/`, `src/client/`, `cordis.patch.yml`: real Harness tool/model/authenticated-web integration and lazy pane. No Harness source fork/change.
- `src/client/theme-context.mjs` reads the optional official `theme.getTheme()` service and subscribes to `theme/change` only while the iframe is visible. It resolves 38 allowlisted presentation variables from computed host styles; narrow root/body attribute observation covers typography and bootstrap changes without polling. `web/theme.js` verifies the exact same-origin parent, applies the current mode/tokens and releases listeners on disposal; standalone uses `prefers-color-scheme`. `web/theme.css` maps the interface to DSH semantic tokens and supplies standalone light/dark fallbacks. PDF images and annotation colors are not theme-filtered.
- `src/local-state.mjs` stores bounded CAS-versioned records under the resolved DSH home, namespaced by the canonical library path. It uses private modes, cross-process locks, file/directory sync and atomic replacement; runtime peers reuse DSH home/atomic-write utilities. `web/local-state.js` owns asynchronous restoration, queued saves, conflict recovery and verified legacy migration. Durable records are never an LRU cache. Changing a library path changes its namespace; migration across moved libraries is not automatic.
- `src/harness/language-learning.mjs` resolves the paper Session model before generation, validates source-backed vocabulary, checkpoints results before idempotent merging, and replays completed request IDs without another model call. Pending/uncertain and failed requests have distinct recovery paths. `web/language-learning.js` owns per-paper language drafts, history and vocabulary review. Results are local records; adding them to a question prepares the correct paper's draft without sending it or changing its PDF.
- `src/harness/paper-chat.mjs`: stable paper-to-Session identity, cold Session preparation, exact reference context, native admission, log-confirmed usage and bounded history. `annotation-snapshots.mjs` stores immutable private source snapshots and request bindings; `paper-reference-resolver.mjs` resolves direct-user tokens through the public pre-step waterfall; `annotation-usage.mjs` folds verified source envelopes and hashes into the native Session projection. Standard PDF annotations remain authoritative.
- `src/client/conversation-context.mjs` owns revision-guarded main-composer draft/navigation and a 256 KiB reader handoff. `annotation-references.mjs` registers the official input reference source/codec and keeps only one on-demand snapshot inspector. Main-input chips preserve other text/references/attachments; refreshed drafts remain resolvable plain tokens with an inspector entry. The inspector uses additive `conversation.input.dock`, and main history retains the native generic context renderer.
- `web/`: framework-free interface. `pdf-reader.js` keeps at most three page raster/text layers and one serialized geometry/page request; distant pages retain geometry placeholders only. `reading-panels.js` moves the original library/annotation nodes into one tabbed rail, restores catalog mode and owns metadata quick editing/floating chat; `reading-shell.js` owns the contextual ribbon/fullscreen. Coordinator tests cover selected-paper identity, hidden chat polling and closed-panel restoration. Metadata/chat/language drafts are separate host-owned per-paper records capped at 256 KiB each, with a bounded client working set. Raw selection, annotation type/color and panel state join the bounded iframe handoff; host records take precedence over old handoffs. An open annotation editor prevents an external reference from navigating away until the reader finishes or closes it.
- `scripts/`: build, install/audit, fixture creation, validation and benchmarks. Private data/outputs are ignored.

## Verified evidence

[Thirteen shared-sidebar flows](docs/validation/sidebar-browser.json) verify compact cards, one library/annotation rail, stable PDF position, note navigation/edit/save, metadata and chat draft preservation, table management, and narrow/fullscreen recovery in light/dark mode. Eight new shared-rail unit cases cover original node identity, preserved state, catalog restoration, side preference, close behavior, tab keyboard roles, owner initialization and disposal. The six legacy panel cases remain. These are synthetic functional/rendered checks, not real-library capacity or formal user validation.

[Twelve theme browser checks](docs/validation/theme-browser.json) cover 430/741/1400 px in light/dark mode across catalog, reader sidebars, language, metadata, graph and dialogs, plus keyboard focus, errors and sampled text contrast. System-theme changes and host bridge updates preserve the open reader and unsaved draft without reloading the iframe. Eleven protocol/lifecycle unit tests cover official mode/font authority, bounded tokens, message source validation, retraction, coalescing and cleanup. These are functional/rendered checks with synthetic documents, not a formal accessibility certification, native settings walkthrough or user study.

[Validation report](docs/validation.md) links source receipts and current automated counts. Language cases cover authoritative model selection, request identity, interrupted commits, exact vocabulary provenance and preserved manual review. State cases cover bounded private writes, concurrent CAS, unsafe paths, failed saves, UTF-8 limits and legacy migration. The actual cross-browser fixture disables browser-storage writes and verifies drafts, page position, history, vocabulary and conflict export. Actual Harness restart preserves records and replays a completed generation without another call. Reading, catalog, graph, reference, conversation and intake tests remain. Earlier test counts are historical milestones.

Thirteen real isolated Harness checks pass in `docs/validation/paper-chat-harness.json`. Besides cold Sessions, distinct papers, real page-two sources, retry deduplication and PDF reply provenance, the fixture proves that draft preparation does not mark notes sent, later PDF edits leave the prepared snapshot unchanged, the model receives the frozen source, updated PDF versions remain distinguishable, and native main-composer plain-token submission updates usage without the plugin's send action. The fixture made two deterministic local generations and zero external model requests.

The current [browser receipt](docs/validation/annotation-reference-browser.json) passes 11 flows over 46 synthetic annotations, including filtered multi-select, separate paper drafts, later-note suggestions, changed-version adoption, new/sent mixing, all-note native chip insertion, reload to a plain token, source inspector/page navigation, main-composer sending, sent-state reconciliation and missing-snapshot errors. It uses three deterministic local generations and records no browser errors. Both narrow/wide selection views and the expanded native inspector pass horizontal-overflow checks. The previous ten-flow conversation walkthrough and PDF reply-save checks remain in `docs/validation/paper-chat-manual.json`. The current installed-host check also passed; see `docs/validation/annotation-reference-install.json`.

Earlier verification also included nine actual web-host checks for the bundled skill path, model directory and authenticated routes without submitting a model request. These remain historical plugin-load evidence.

Intake browser walkthrough: pasted the public W3C dummy PDF URL, observed its saved managed filename and readable page; selected two generated PDFs together and observed sequential automatic import; an invalid remote target preserved the input and exposed a retry. Browser error logs were empty. Drag events, per-file failure continuation and editable-text paste preservation are covered by actual UI-handler tests; OS drag automation was not used. See `docs/validation/intake-manual.json`.

The earlier browser walkthrough verified import display, APA clipboard, page note, text highlight, persistence after reload, graph creation/provenance and full-library export. Its deterministic standalone-feedback check covered model-adapter → PDF → fresh-library recovery. This is compatibility-path evidence; it does not validate the new conversation UI. No live model-quality evaluation or formal usability study is claimed.

The earlier 2,000-record Harness browser fixture verified the shared-composer route A → B → A and preservation of an unsaved Chinese annotation draft. That continuously-following route was used by standalone feedback; the current paper conversation instead retains its own model after first creation. See `tests-js/fixtures/harness-models/README.md` for the historical selection-only fixture.

The current [reference-memory receipt](docs/validation/annotation-reference-memory.json) uses a verified copy of 2,000 synthetic records and 1,000 four-page PDFs. One 45-note catalog and exact snapshot, 20 additional cold papers and 30 history polls leave 21 cold Sessions, zero Agents, zero model calls and zero idle workers. History polls open zero PDFs but still run metadata-only workers. Host settled RSS is 569.88 MiB, with a 602.17 MiB sampled maximum; the exact PDF worker's true `ru_maxrss` peak is 58.14 MiB. Browser memory and benchmark-driver RSS are excluded. There is no matched baseline or forced GC, so RSS decreases are not feature memory savings. Reproduce with `node scripts/benchmark-annotation-references.mjs`.

The earlier capacity measurements used the same small-text-PDF scale before automatic intake and native conversations: search worker peak about27 MiB, PDF worker63–68 MiB and Harness+plugin snapshots about93 MiB after searches /202 MiB after reading. Those separate runs are not a matched comparison with current RSS. Do not claim total desktop memory or real-library validation.

## Installation and Git boundary

The shared-sidebar [live-asset receipt](docs/validation/sidebar-install.json) checks authenticated updated reading assets and retained host capabilities. A review page with unresolved unsaved state must remain open; inspect an update in another page until that state is safely resolved. The sidebar update adds no storage migration or model request.

The theme update's [installed-host receipt](docs/validation/theme-install.json) verifies the restarted authenticated host, theme assets and retained conversation/reference/reader/language/state capabilities. This check made no model request and read no private document. Browser theme propagation is separately verified with the isolated fixture described above; it is not claimed as a full native settings UI test.

The language update's [installed-host check](docs/validation/language-install.json) confirms authenticated `durableState` and `languageLearning`, all three new browser assets and retained reader/conversation/reference capabilities. The existing idle web host and standalone review service were restarted without configuration changes. The standalone language panel was opened visibly; native desktop-window refresh is not claimed. Its legacy migration reported one conflicting browser record backed up locally and exposed the export entry; no record contents or private documents were exported during verification.

The earlier version was registered once in the existing `web` profile through the official additive CLI, followed by an additive per-paper-conversation upgrade. The prior host was checked idle and restarted on its existing port; authenticated checks and the refreshed native PWA verified that earlier version without opening private papers or requesting a model. See `docs/validation/install.json` and `docs/validation/paper-chat-install.json`. The continuous-reference update has now passed its own [installation check](docs/validation/annotation-reference-install.json): `annotationReferences:true`, preserved existing configuration and plugins, authenticated assets, and the refreshed native library pane. No private PDF was opened and no model request was made for this installation check. Local peer symlinks target a built Harness checkout; `npm ci` can remove them, so rerun the install helper after refreshing dependencies.

Public repository: [mappedinfo/dsh-paper-library](https://github.com/mappedinfo/dsh-paper-library). The owner requested public GitHub publication and MIT for original project code on 2026-09-14. Third-party terms remain intact: default PyMuPDF and citeproc runtime dependencies use AGPL, and bundled CSL assets use CC BY-SA. See `THIRD_PARTY.md`; do not describe the full installation as MIT-only.

The authorized community showcase is published in **Show Your Plugins!**, [upstream Discussion #6623](https://github.com/deepseek-ai/deepseek-harness/discussions/6623). The title, body, category, author and two public screenshot files were read back and verified. The post explicitly labels the project unofficial, explains native DSH integration and current limits, and uses only synthetic documents in standalone preview screenshots. `docs/community/` stores the reviewed post, publication receipt and reproduction instructions; `scripts/publish-discussion.mjs` reuses the existing topic rather than creating duplicates. The temporary screenshot server was stopped after publication; the user's installed Harness host was left running.

The initial two commits and current tree were audited before publication: no real literature, managed PDFs, catalogs, credentials, private config or user absolute paths were tracked. Reproduce the bounded file/history/package checks with `npm run check:publication`; the receipt is `docs/validation/publication.json`. Historical AGPL root notices reflect the earlier original-code license; the current original-code grant is MIT. Keep runtime data ignored. Source changes are committed at completion; ordinary later pushes follow the repository's existing upstream and the active task authorization. No actual Zotero migration or new scheduler was performed. Standalone and isolated test hosts were stopped after verification.

## Known limits and next validation

Language output and suggested vocabulary need reader judgment. Generation sends the explicitly selected text to the paper's configured DSH model and consumes that model's quota; local persistence does not mean local model inference. Standalone preview can review records but generation requires DSH. Records are bounded at 256 KiB; saves below 60,000 UTF-8 request bytes use keepalive, while larger pending saves require the page to stay connected. Abrupt host/browser termination before acknowledgment cannot guarantee saving unsent text. Malformed or oversized legacy browser drafts remain in place with a migration error rather than being silently deleted.

**Accepted reference update (2026-09-14, implemented):** [Continuous annotation references](docs/annotation-reference-design.md) now documents current behavior. The accepted implementation adds additive selection, exact source budgets, frozen snapshots, logged usage and official input references. Automatic batch reading, a native per-user-message custom card, and automatic chip reconstruction after reload remain unimplemented. Do not revive the old replace-only selection or silent 40-note/1,500-character truncation in native chat.

**Accepted implementation complete:** automated, native integration, browser, scoped memory and existing-profile installation checks all pass. Follow the boundaries below for additional validation; they are not claims of real-library, WPS or provider-quality coverage.

1. **WPS viewing/saving:** native application automation failed to complete opening the synthetic copy. Validate standard highlights/comments and embedded metadata after WPS save on a copied file; do not claim WPS compatibility from PyMuPDF-only tests. Existing user documents were not edited.
2. **Real library:** benchmark a user-selected export/copy, including scanned/image-heavy PDFs and long reading sessions. Keep total browser/host/worker memory separate. Do not infer Zotero's 3 GB cause from this implementation.
3. **Search:** title/author/DOI/citekey/tags/abstract only. Full-text indexing/OCR/embeddings are deferred; add an explicit on-demand pipeline and memory budget if requested.
4. **Migration:** JSON metadata batch cap2,000/32MB, process100 at a time; PDF250MB/2,000pages; one managed PDF per record. Export DB-only Zotero annotations into PDF or provide JSON positions. Collections, standalone notes and dynamic word-processor fields are not migrated. Duplicate PDFs are not automatically merged/replaced.
5. **Conversation coverage:** the reference update has automated, actual Host and 11-flow browser evidence, with the browser flows rerun after the workbench changes. The expanded native inspector was checked against viewport and PDF-frame bounds. Authenticated installed-host capability checks pass. Actual-library mutation testing and real-provider response quality remain outside the verified scope.
6. **Conversation limits:** inline history contains at most 20 committed user/assistant texts, 6,000 characters per message and 48,000 total. Full history, live tool output, approvals and stop controls remain in the main conversation. Cold reads close their observer and leave Harness's bounded prepared-session cache to its owner. First creation checkpoints title and blank-list metadata through the optional public cache service after the log is durable, without activating an Agent. Harness deliberately hides noncurrent zero-turn Sessions, labels the selected empty one “New Session” and hides its main header until prompting; the plugin's “Open main conversation” still opens it by identity. The paper title appears normally after the first message. Native Agents activated by main-page opening or prompting remain under Harness lifecycle management; no plugin-owned idle eviction is claimed. Archived paper Sessions produce an explicit error because the current public API has no unarchive operation. Moving the library directory does not migrate its deterministic paper-to-Session identity.
7. **PDF feedback:** explicit saves read a completed native assistant event on the server, require an attached PDF, and deduplicate by Session/message ID. The stored note records provenance and uses an empty annotation association list; the plugin does not infer which earlier annotation a later answer addresses. Older responses outside the inline history window cannot currently be saved from the inline UI. Actual provider usefulness and long-running conversation memory remain unmeasured. The standalone `ai_feedback`/`library_feedback` path remains for compatibility and is not the default Harness reading workflow.
8. **Acquisition:** direct public PDFs, Crossref/DOI, arXiv and generic citation metadata are supported. Publisher-specific authenticated/browser providers and OCR remain deferred. Exact parsed-title matching can decline enrichment for truncated or unusual titles; keep the review flag rather than guessing. Live network smoke used a public synthetic PDF; publisher and arXiv branches use deterministic transport fixtures.

Reopening deferred work requires a specific feature/data request, not a silent expansion during routine maintenance.
